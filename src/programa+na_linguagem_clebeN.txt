// Este é um comentário de linha
funtion calculateSum(a, b) {
    assign result to a + b;
    if (result > 1@0) {
        return true;
    } else {
        return false;
    }
}

assign x to 5;
assign y to 8;
assign sum to calculateSum(x, y);